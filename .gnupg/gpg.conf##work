#
# See:
# - man gpg
# - https://help.riseup.net/en/security/message-security/openpgp/best-practices
#

# The default key to sign with
default-key 0x392DE39E58D800CA

# The default recipient
#default-recipient some-user-id
default-recipient-self

# Behavior --------------------------------------------------------------------

# Get rid of the copyright notice
no-greeting

# Always display fingerprints
with-fingerprint

# Display the calculated validity of user IDs during key listings
list-options show-uid-validity
verify-options show-uid-validity

# Show long IDs (prevent collisions)
keyid-format 0xlong

# Keyserver -------------------------------------------------------------------

# Keyservers that is used to send/recv/search keys.
keyserver hkp://keys.gnupg.net

# When using --refresh-keys, if the key in question has a preferred keyserver
# URL, then disable use of that preferred keyserver to refresh the key from
keyserver-options no-honor-keyserver-url

# When searching, include keys that are marked on the keyserver as revoked
keyserver-options include-revoked

# Algorithms and ciphers ------------------------------------------------------

# Set personal digest preferences
personal-cipher-preferences AES256 AES192 AES CAST5
personal-digest-preferences SHA512 SHA384 SHA256 SHA224

# Algorithms used in symmetric encryption
s2k-cipher-algo AES256
s2k-digest-algo SHA512

# Use a strong digest when signing a key
cert-digest-algo SHA512

# Set the default preferences for new keys
default-preference-list SHA512 SHA384 SHA256 SHA224 AES256 AES192 AES CAST5 ZLIB BZIP2 ZIP Uncompressed

# vim: ft=gpg
