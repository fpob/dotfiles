#!/usr/bin/env bash

cdrom=/dev/cdrom

if [[ $1 = '--help' ]] ; then
    cat << EOF
Použití: `basename $0` <target.iso>

Z CD ($cdrom) vytvoří .iso obraz
EOF
    exit 0
fi

[[ -n $1 ]] && { echo "Nebyl zadán výstupní soubor" >&2 ; exit 1 ; }

dd if=$cdrom of="$1"
